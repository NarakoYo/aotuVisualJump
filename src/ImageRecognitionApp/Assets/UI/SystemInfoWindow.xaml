<Window x:Class="ImageRecognitionApp.Assets.UI.SystemInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ImageRecognitionApp.Assets.UI"
        Title="系统信息"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        Foreground="White"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0,8,0,0" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Border Background="#1E1E1E" CornerRadius="8" BorderBrush="#2D2D2D" BorderThickness="1">
            <Grid>
                <!-- 标题栏 - 只包含关闭按钮 -->
                <Border Height="40" VerticalAlignment="Top" Background="#252526" CornerRadius="8,8,0,0">
                    <Button x:Name="CloseButton" Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click" HorizontalAlignment="Right">
                        <TextBlock Text="×" FontSize="16"/>
                    </Button>
                </Border>
                
                <!-- 内容区域 -->
                <Grid Margin="0,40,0,0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="Transparent" Padding="16">
                        <StackPanel Orientation="Vertical" Width="Auto" Height="Auto">
                            <TextBlock Text="系统信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- .NET Framework版本 -->
                                <TextBlock Text="框架版本:" Grid.Column="0" Grid.Row="0" Margin="0,8,0,8"/>
                                <TextBlock x:Name="FrameworkVersionText" Grid.Column="1" Grid.Row="0" Margin="0,8,0,8"/>
                                
                                <!-- 操作系统信息 -->
                                <TextBlock Text="操作系统:" Grid.Column="0" Grid.Row="1" Margin="0,8,0,8"/>
                                <TextBlock x:Name="OSVersionText" Grid.Column="1" Grid.Row="1" Margin="0,8,0,8"/>
                                
                                <!-- 系统架构 -->
                                <TextBlock Text="系统架构:" Grid.Column="0" Grid.Row="2" Margin="0,8,0,8"/>
                                <TextBlock x:Name="ArchitectureText" Grid.Column="1" Grid.Row="2" Margin="0,8,0,8"/>
                                
                                <!-- 系统内存 -->
                                <TextBlock Text="系统内存:" Grid.Column="0" Grid.Row="3" Margin="0,8,0,8"/>
                                <TextBlock x:Name="MemoryText" Grid.Column="1" Grid.Row="3" Margin="0,8,0,8"/>
                                
                                <!-- CPU信息 -->
                                <TextBlock Text="CPU信息:" Grid.Column="0" Grid.Row="4" Margin="0,8,0,8"/>
                                <TextBlock x:Name="CPUText" Grid.Column="1" Grid.Row="4" Margin="0,8,0,8"/>
                                
                                <!-- 屏幕分辨率 -->
                                <TextBlock Text="屏幕分辨率:" Grid.Column="0" Grid.Row="5" Margin="0,8,0,8"/>
                                <TextBlock x:Name="ResolutionText" Grid.Column="1" Grid.Row="5" Margin="0,8,0,8"/>
                                
                                <!-- 应用程序版本 -->
                                <TextBlock Text="应用版本:" Grid.Column="0" Grid.Row="6" Margin="0,8,0,8"/>
                                <TextBlock x:Name="AppVersionText" Grid.Column="1" Grid.Row="6" Margin="0,8,0,8"/>
                                
                                <!-- 应用程序路径 -->
                                <TextBlock Text="应用路径:" Grid.Column="0" Grid.Row="7" Margin="0,8,0,8"/>
                                <TextBlock x:Name="AppPathText" Grid.Column="1" Grid.Row="7" Margin="0,8,0,8" TextWrapping="Wrap"/>
                            </Grid>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>